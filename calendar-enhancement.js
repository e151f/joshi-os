(()=>{const root=()=>document.querySelector('#view'),APP='elif-os-v2-state',TR='elif-os-trading-v1',MEAL='elif-os-meal-v2',JOURNAL='elif-os-journal-v2';const pad=n=>String(n).padStart(2,'0'),key=d=>d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate()),read=k=>{try{return JSON.parse(localStorage.getItem(k)||'{}')}catch{return{}}},esc=s=>String(s??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
const COURSES={1:[['09:00','12:00','PHAR205 - Farmasötik Mikrobiyoloji','GR01 · ST110 · Teorik']],2:[['16:00','19:00','PHAR205 - Farmasötik Mikrobiyoloji','GR01 · STB09 · Uygulama']],3:[['10:00','11:00','PHAR201 - Analitik Kimya II','GR02 · ST263 · Teorik'],['11:00','13:00','CHEM213 - Biyokimya','GR01 · ST119 · Teorik'],['15:00','18:00','PHAR201 - Analitik Kimya II','GR02 · STB11 · Chemistry Lab']],4:[['09:00','10:00','HELS211 - Halk Sağlığı','GR01 · ST237 · Teorik'],['11:00','12:00','PHAR207 - Eczacılık Uygulamaları','GR01 · ST237 · Teorik'],['13:00','14:00','PHAR201 - Analitik Kimya II','GR02 · ST236 · Teorik']],5:[['10:00','12:00','ANAT120 - Anatomi-Fizyoloji-II','GR01 · ST237 · Teorik']]};
function coursesFor(k){const d=new Date(k+'T12:00:00'),items=COURSES[d.getDay()]||[];return items.map(x=>({time:x[0],end:x[1],text:x[2]+' · '+x[3],course:true}))}
function cleanOldSchedule(){try{const s=read(APP);if(s.calendarScheduleVersion==='20260926-current')return;const codes=['TARH101','PHAR201','PHAR205','PHAR207','ANAT120','CHEM213','HELS211','APSC123'];if(Array.isArray(s.calendarEvents))s.calendarEvents=s.calendarEvents.filter(x=>{const z=String((x&&x.title)||'');return x?.source!=='course-schedule'&&!codes.some(code=>new RegExp('(^|\\s)'+code+'(\\s|$)').test(z))});s.calendarScheduleVersion='20260926-current';localStorage.setItem(APP,JSON.stringify(s))}catch{}}
let cursor=new Date(),selected=key(new Date());cleanOldSchedule();
function activities(k){const s=read(APP),out=coursesFor(k);if(s.tasks)s.tasks.filter(x=>x.date===k).forEach(x=>out.push({time:'',text:'Task: '+x.title+(x.done?' ✓':'')}));if(s.habits)s.habits.filter(x=>x.status!=='on hold'&&s.habitLog&&s.habitLog[x.id]&&s.habitLog[x.id][k]).forEach(x=>out.push({time:'',text:'Habit: '+x.name+' ✓'}));if(s.studySessions)s.studySessions.filter(x=>x.date===k).forEach(x=>out.push({time:x.start?new Date(x.start).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):'',text:'Study · '+x.minutes+' min'}));if(s.studyLog&&s.studyLog[k]&&!out.some(x=>x.text.indexOf('Study')===0)&&Number(s.studyLog[k]))out.push({time:'',text:'Study · '+s.studyLog[k]+' min'});if(s.water&&s.water[k])out.push({time:'',text:'Water · '+s.water[k]+' L'});if(s.sleep&&s.sleep[k])out.push({time:'',text:'Sleep · '+s.sleep[k]+' h'});if(s.movementLog&&s.movementLog[k])out.push({time:'',text:'Workout · '+s.movementLog[k]});const personal=Array.isArray(s.calendarEvents)?s.calendarEvents.filter(x=>x.date===k):[];personal.forEach(x=>out.push({time:x.start||'',text:'Event: '+x.title}));const t=read(TR);if(t.trades)t.trades.filter(x=>x.date===k).forEach(x=>out.push({time:x.time||'12:18',text:'Trade · '+x.asset+' · '+(x.quantity||41)+' pay · Net P/L '+(Number(x.pnl||0)>=0?'+':'')+'$'+Number(x.pnl||0).toFixed(2)}));const m=read(MEAL);if(m.plans&&m.plans[k])Object.keys(m.plans[k]).forEach(type=>out.push({time:'',text:'Meal: '+type+' · '+m.plans[k][type]}));const j=read(JOURNAL);if(j.entries)j.entries.filter(x=>x.date===k).forEach(x=>out.push({time:'',text:'Journal: '+x.title}));return out}
function monthGrid(){const y=cursor.getFullYear(),m=cursor.getMonth(),first=new Date(y,m,1),last=new Date(y,m+1,0),start=(first.getDay()+6)%7,days=[];for(let i=0;i<start;i++)days.push('<div class="cal-day muted"></div>');for(let n=1;n<=last.getDate();n++){const d=new Date(y,m,n),k=key(d),count=activities(k).length;days.push('<button type="button" class="cal-day '+(k===key(new Date())?'today ':'')+(k===selected?'selected':'')+'" data-cal-day="'+k+'"><span class="cal-num">'+n+'</span>'+(count?'<span class="cal-dot">'+count+' activity</span>':'')+'</button>')}return days.join('')}
function render(){const v=root();if(!v||v.dataset.page!=='calendar')return;const title=cursor.toLocaleDateString('en',{month:'long',year:'numeric'}),items=activities(selected);let rows='';if(items.length)items.forEach(x=>{rows+='<div class="row"><time style="width:60px;color:var(--pink)">'+esc(x.time)+'</time><span class="grow">'+esc(x.text)+'</span></div>'});else rows='<div class="empty-state">Nothing logged for this day yet.</div>';v.innerHTML='<section><div class="viewhead"><div><h1>Calendar</h1><p>Current class schedule, activities and daily logs.</p></div><button class="primary" type="button" data-cal-today>today</button></div><div class="panel calendar-wrap"><div class="calendar-head"><button class="ghost" type="button" data-cal-prev>←</button><div class="calendar-title">'+title+'</div><button class="ghost" type="button" data-cal-next>→</button></div><div class="calendar-grid"><div class="cal-dow">MON</div><div class="cal-dow">TUE</div><div class="cal-dow">WED</div><div class="cal-dow">THU</div><div class="cal-dow">FRI</div><div class="cal-dow">SAT</div><div class="cal-dow">SUN</div>'+monthGrid()+'</div></div><div class="panel" style="margin-top:14px"><div class="section-title"><span>'+new Date(selected+'T12:00:00').toLocaleDateString('en',{weekday:'long',day:'numeric',month:'long'})+'</span><small>'+items.length+' activities</small></div><div class="list" style="margin-top:10px">'+rows+'</div></div></section>'}
document.addEventListener('click',e=>{const b=e.target.closest&&e.target.closest('[data-cal-day],[data-cal-prev],[data-cal-next],[data-cal-today]');if(!b||!root()||root().dataset.page!=='calendar')return;e.preventDefault();e.stopImmediatePropagation();if(b.dataset.calDay){selected=b.dataset.calDay;render()}else if(b.hasAttribute('data-cal-prev')){cursor.setMonth(cursor.getMonth()-1);render()}else if(b.hasAttribute('data-cal-next')){cursor.setMonth(cursor.getMonth()+1);render()}else{cursor=new Date();selected=key(cursor);render()}},true);window.addEventListener('click',e=>{if(e.target.closest&&e.target.closest('button[data-page="calendar"]'))setTimeout(render,0)});window.ELIFCalendarEnhancement={render};render()})();